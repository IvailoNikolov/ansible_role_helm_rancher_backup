apiVersion: v1
kind: Secret
metadata:
  name: s3-creds
  namespace: "{{ helm_rancher_backup_release_namespace }}"
type: Opaque
stringData:
  accessKey: "{{ rancher_backup_accessKey }}"
  secretKey: "{{ rancher_backup_secretKey }}"